package io.aime.util;

// --- AIME
import io.aime.net.URLFilters;

// --- Apache Hadoop
import org.apache.hadoop.conf.Configuration;

public class Filters {

  /**
   * The URL filters local object.
   */
  private URLFilters filters;

  /**
   * This method initializes the filters through the Filter's view window.
   * <p>What this method does is to start the filters locally, so that the Cerebellum
   * can serve them to all nodes of AIME.</p>
   * <p>This method is made for launching from it's view only.</p>
   *
   * @param conf Configuration's object.
   */
  public void startLocalFilters (Configuration conf) {
    // Init the URL filters.
    this.filters = new URLFilters (conf);
  }

  /**
   * Return the local filters.
   *
   * @return The local filters.
   */
  public URLFilters getFilters () {
    return this.filters;
  }
}
