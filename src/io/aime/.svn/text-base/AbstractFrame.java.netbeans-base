package io.aime;

// AIME
import io.aime.mvc.controller.AbstractController;
import io.aime.mvc.model.AbstractModel;
import io.aime.mvc.view.AbstractViewPanel;

// Swing
import javax.swing.JFrame;

/**
 * Base class for all Frames inside AIME.
 *
 * @author K-Zen
 * @see Builder Pattern
 */
public abstract class AbstractFrame extends JFrame {

    /**
     * Creates the GUI.
     */
    protected abstract AbstractFrame createGUI();

    /**
     * Configure this Frame.
     */
    protected abstract AbstractFrame configure();

    /**
     * Makes the GUI visible. That involves creating it if it's not already created.
     */
    public abstract AbstractFrame makeVisible();

    /**
     * Returns a controller associated with this Frame.
     *
     * @param id The unique ID of the controller.
     *
     * @return The controller.
     */
    protected abstract AbstractController getController(byte id);

    /**
     * Returns a model associated with this Frame.
     *
     * @param id The unique ID of the model.
     *
     * @return The model.
     */
    protected abstract AbstractModel getModel(byte id);

    /**
     * Returns a view associated with this Frame.
     *
     * @param id The unique ID of the view.
     *
     * @return The view.
     */
    protected abstract AbstractViewPanel getView(byte id);
}
